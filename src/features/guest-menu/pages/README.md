# Guest Menu Boundary

The public route remains `src/pages/GuestMenu.jsx`, which is now a compatibility facade. New guest menu hooks, cart behavior, order tracking, and presentation components should live in `src/features/guest-menu`.
