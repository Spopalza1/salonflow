# Application Contracts

UI modules call application services. Application services use the configured backend port. `base44Backend` is the production adapter today; a local HTTP adapter can be selected later without changing feature components.
