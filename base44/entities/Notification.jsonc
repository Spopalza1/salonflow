{
  "name": "Notification",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "order",
        "service",
        "service_note",
        "guest_message",
        "chat",
        "service_update"
      ],
      "default": "order"
    },
    "read": {
      "type": "boolean",
      "default": false
    },
    "salon_id": {
      "type": "string"
    }
  },
  "required": [
    "title",
    "body",
    "type"
  ],
  "rls": {
    "create": true,
    "read": {
      "data.salon_id": "{{user.data.salon_id}}"
    },
    "update": {
      "data.salon_id": "{{user.data.salon_id}}"
    },
    "delete": {
      "data.salon_id": "{{user.data.salon_id}}"
    }
  }
}